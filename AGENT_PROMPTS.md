# FundCare Agent Prompts

## AI 组合分析报告 Prompt 设计

FundCare 在生成 AI 报告时，会把组合规模、估算盈亏、主题分布、新闻情绪和沪深300基准信息传给 DeepSeek API，并要求模型返回结构化 JSON。前端固定展示为六个模块：组合概览、主要风险、市场情绪、主题结构、优化方向和注意事项。

要求模型：

- 不输出买入、卖出、加仓、减仓等明确交易指令；
- 不做收益承诺；
- 只进行信息整理、风险提示和结构分析；
- 每个模块返回数组，便于前端分点展示。

## AI 图片识别辅助导入 Prompt 设计

最终版根据课堂展示后的修改意见，在 01 持仓录入模块中加入 AI 图片识别辅助导入。流程为：

1. 前端使用 Tesseract.js 对持仓截图进行 OCR；
2. 将 OCR 原始文字发送给 `/api/ocr-ai`；
3. 后端调用 DeepSeek API，将 OCR 文本整理为结构化 JSON；
4. 前端展示待确认列表；
5. 用户确认后再导入持仓表。

识别 Prompt 要求：

- 只提取基金代码、基金名称和持仓金额；
- 不把昨日收益、持有收益、涨跌幅误认为持仓金额；
- 无法确定的字段允许留空；
- 返回 JSON，不返回 Markdown；
- 识别结果必须人工确认后再导入。

该模块定位为“辅助导入”，不是完全自动录入，主要用于降低手动输入成本并避免识别误差直接影响分析结果。
